<!DOCTYPE html>
<html lang="en">
    <% console.log(locals) %> <%- include('partials/head', {title: `Yuna | ${file.name}`, cssPath: '/styles/file.css'}) %>
    <!---->
    <%- include('navBar', {authenticated}) %>
    <body>
        <% console.log(locals) %>
        <h1><%= file.name %></h1>
        <a href="<%= file.url %>?download=<%= file.name %>">download file</a>
    </body>
</html>
